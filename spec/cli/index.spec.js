var cli = require('../../lib/cli');

describe('cli/index', function() {
    it('should be require-able', function() {
       // expect(cli).to.not.equal(null);
    });

    it('should have argv', function() {
       // expect(cli.argv).to.not.equal(null);
    });

    it('should have help', function() {
       // expect(cli.help).to.not.equal(null);
    });

    it('should have version', function() {
        //expect(cli.version).to.not.equal(null);
    });
});